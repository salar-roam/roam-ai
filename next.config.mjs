// next.config.mjs
/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true, // Recommended for detecting problems
    // You can add other configurations here if needed,
    // like 'images' for external image domains.
  };
  
  export default nextConfig;